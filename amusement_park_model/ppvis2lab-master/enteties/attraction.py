import Visitor


class Attraction(object):
    def __init__(self):
        session_time = int()
        capacity = int()
        price = int()
        queue = dir(Visitor)
        coordinats = (int(), int())
